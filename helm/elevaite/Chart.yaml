apiVersion: v2
name: elevaite
description: ElevAIte AI Workflow Automation Platform
type: application
version: 0.1.0
appVersion: "1.0.0"

keywords:
  - ai
  - workflow
  - automation
  - agents

maintainers:
  - name: ElevAIte Team
    email: team@elevaite.ai

dependencies:
  # Optional: use Bitnami charts for production-grade data services
  # Uncomment to use instead of bundled templates
  # - name: postgresql
  #   version: "~13.0"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: postgresql.enabled
  # - name: rabbitmq
  #   version: "~12.0"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: rabbitmq.enabled
  # - name: minio
  #   version: "~12.0"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: minio.enabled

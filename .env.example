# =============================================================================
# ElevAIte - SINGLE Environment File for ALL Services
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git! It contains secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - LLM API Keys
# -----------------------------------------------------------------------------
# At least one LLM provider is required for AI features
OPENAI_API_KEY=sk-proj-your-openai-key-here

# Optional: Gemini API key for Google AI features
# GEMINI_API_KEY=your-gemini-key-here

# Optional: Anthropic API key for Claude
# ANTHROPIC_API_KEY=your-anthropic-key-here

# -----------------------------------------------------------------------------
# REQUIRED - Google OAuth (for login)
# -----------------------------------------------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# -----------------------------------------------------------------------------
# Frontend URLs (local dev defaults)
# -----------------------------------------------------------------------------
# Auth frontend app (port 3005)
AUTH_URL=http://localhost:3005
AUTH_API_URL=http://localhost:8004
NEXTAUTH_URL_INTERNAL=http://localhost:3005
RBAC_BACKEND_URL=http://localhost:8004

# Elevaite frontend app (port 3001)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8006
NEXT_PUBLIC_NATIVEAUTH_URL=http://localhost:8004
NEXT_PUBLIC_AUTH_API_URL=http://localhost:8004
NEXT_PUBLIC_MODELS_API_URL=http://localhost:8006
NEXT_PUBLIC_CONTRACTS_API_URL=http://localhost:8006
NEXT_PUBLIC_CONTRACTS_IOPEX_API_URL=http://localhost:8006

# -----------------------------------------------------------------------------
# OPTIONAL - Auth Configuration
# -----------------------------------------------------------------------------
# JWT secret for token signing (auto-generated if not set)
# JWT_SECRET=your-super-secret-jwt-key

# Session secret for cookie signing
# AUTH_SECRET=your-auth-secret

# -----------------------------------------------------------------------------
# OPTIONAL - External Services
# -----------------------------------------------------------------------------

# Slack notifications (for workflow triggers)
# SLACK_BOT_TOKEN=xoxb-your-token
# SLACK_SIGNING_SECRET=your-signing-secret

# Email sending (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# -----------------------------------------------------------------------------
# OPTIONAL - Qdrant Cloud (Vector Database)
# -----------------------------------------------------------------------------
# For production, use Qdrant Cloud instead of local container
# QDRANT_HOST=your-cluster.qdrant.io
# QDRANT_PORT=6333
# QDRANT_API_KEY=your-qdrant-api-key

# -----------------------------------------------------------------------------
# LOCAL DEV DEFAULTS (usually don't need to change)
# -----------------------------------------------------------------------------
# These are pre-configured in docker-compose.dev.yaml
# Only override if running services outside Docker

# Database URLs (set in docker-compose, not needed here)
# DATABASE_URL=postgresql://elevaite:elevaite@localhost:5433/elevaite

# Redis (set in docker-compose, not needed here)
# REDIS_HOST=localhost
# REDIS_PORT=16379

# MinIO/S3 (set in docker-compose, not needed here)
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin

# RabbitMQ (set in docker-compose, not needed here)
# RABBITMQ_URL=amqp://elevaite:elevaite@localhost:5672/
# URL of the main ElevAIte app (for redirects after login)
ELEVAITE_HOMEPAGE=http://localhost:3001

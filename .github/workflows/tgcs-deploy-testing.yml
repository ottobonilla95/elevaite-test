# IMPORTANT:
# This workflow should never be called on the develop branch.
# However, GitHub requires that we retain a skeleton in place because it determines the UI access based on the primary branch.

name: TGCS - Deploy to Testing

on:
  # Automatic deploy on push or merge to tgcs-main branch
  # Or, optional manual trigger
  push:
    branches: [ tgcs-main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  # don't allow concurrent builds to env
  group: tgcs-deploy-testing
  cancel-in-progress: true

jobs:
  wrong-workflow-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Block execution on non-tgcs branches
        run: |
          # Issue special GitHub error message that appears as an Annotation on the failed workflow.
          echo "::error::❌ WRONG BRANCH SELECTED. Only run TGCS workflows from TGCS branches (tgcs-main, tgcs-release, or feature branches)."
          exit 1
